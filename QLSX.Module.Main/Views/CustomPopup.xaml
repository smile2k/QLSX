<UserControl  xmlns:prism="http://prismlibrary.com/" x:Class="QLSX.Module.Main.Views.CustomPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QLSX.Module.Main.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
              
        >
    
        <UserControl.Resources>
        <Style TargetType="Button" x:Key="CustomButton1">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="5"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />

                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="LightBlue" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#DDE4F0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontFamily" Value="Poppins"/>
            <Setter Property="Width" Value="80"/>
            <!--<Setter Property="Height" Value="29"/>-->
            <Setter Property="Margin" Value="5,0,5,0"/>
        </Style>  
    </UserControl.Resources>
    
    <!--<Border BorderThickness="1" BorderBrush="Black" Padding="10">
        <StackPanel>
            <TextBlock Text="{Binding Message}" FontSize="16" TextAlignment="Center" Margin="10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="OK" Command="{Binding ConfirmCommand}" Width="80" Margin="5"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Border>-->
    
    <Border Background="#d1e4f5" CornerRadius="0" BorderBrush="Gray" BorderThickness="0" Padding="20"
                Width="450" Height="200"
                Visibility="{Binding IsPopupVisible}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!--Title-->
                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" 
                       HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,10"/>

                <!--Message-->
                <TextBlock Text="{Binding Message}" FontSize="14" TextWrapping="Wrap" 
                       Grid.Row="1" HorizontalAlignment="Center" Margin="0,10"/>

                <!--Buttons-->
                <!--Visibility="{Binding ShowCancelButton, Converter={StaticResource BoolToVisibilityConverter}}"-->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0">
                    <Button Content="Đồng ý" Command="{Binding ConfirmCommand}"
                            Style="{StaticResource CustomButton1}"
                            Background="#3e5de9" Foreground="White" BorderThickness="0"/>
                    <Button Content="Hủy" Command="{Binding CancelCommand}" 
                            Style="{StaticResource CustomButton1}"
                            Background="#e76c6f" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>
</UserControl>
